package solver

import (
	"strings"
	"fmt"
)

type Player struct {
	PlayerName string
	Position string
	Salary int
	ProjectedPoints int
}

func AddPlayer (dataLine string) {
	//Expect format QB,Peyton Manning,10,30000
	fmt.Printf("%q\n", strings.Split(dataLine, ","))
}
